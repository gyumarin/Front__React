import React from 'react';
import styles from './Title.module.css';

const Title = (props) => {
  return(
        <img src="../images/douzone_logo.png" alt="DOUZONE LOGO" className={styles.logo}></img>
  );
};
export default Title;